"""Obsolete — the `results` CLI command was removed in favour of the explorer app.

This file intentionally contains no tests and can be deleted
(`git rm tests/test_cli_results.py`). It is left as an empty stub only because
the environment blocks file deletion.
"""
